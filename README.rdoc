== README

This repository contains the program for the InfoVis project, "Your Code is Not Worth Pushing"

In order to run this program, Ruby 2.1+ and Rails is required. 

Other dependencies can be installed by entering 'bundle install'

The server can be run by entering 'rails s', and the program can be accessed at 'http://localhost:3000'
